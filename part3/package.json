{
  "name": "part3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node phonebook.js",
    "phonebook:dev": "nodemon phonebook.js",
    "phonebook:start": "node phonebook.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../part2 && npm run build && cp -r dist ../part3",
    "deploy": "git add . && git commit -m deploy && git push",
    "deploy:full": "npm run build:ui && git add . && git commit -m deploy && git push"


  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}